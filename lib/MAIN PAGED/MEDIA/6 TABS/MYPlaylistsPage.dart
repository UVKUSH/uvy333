import 'package:flutter/material.dart';

class NewRelease extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('My Playlists'),
      ),
      body: Center(
        child: Text('This is the My Playlists page'),
      ),
    );
  }
}
