import 'package:flutter/material.dart';

class HPodcastsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Podcasts'),
      ),
      body: Center(
        child: Text('This is the Podcasts page'),
      ),
    );
  }
}
