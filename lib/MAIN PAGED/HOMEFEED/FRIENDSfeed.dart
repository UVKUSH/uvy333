import 'package:flutter/material.dart';

class FRNDfeed extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      child: Center(
        child: Text('FRND'),
      ),
    );
  }
}
