import 'package:flutter/material.dart';

class MakeReelPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blue, // Set the background color here
      body: Center(
        child: Text(
          'Welcome to MakeReel!',
          style: TextStyle(fontSize: 24.0),
        ),
      ),
    );
  }
}
